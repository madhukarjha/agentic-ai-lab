[metadata]
name = agentic-ai-lab
version = 0.1.0
description = Agentic AI Lab

[options]
packages = find
python_requires = >=3.12

[options.packages.find]
where = .

[options.entry_points]
console_scripts =
    agentic-ai-lab = src.agents.basic_openai_agent:main
